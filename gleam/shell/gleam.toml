name = "glesh"
version = "1.0.0"

# For a full reference of all the available options, you can have a look at
# https://gleam.run/writing-gleam/gleam-toml/.

[dependencies]
# dev
gleam_stdlib = "~> 0.34 or ~> 1.0"
# gleam_stdlib = { path = "../gleam_stdlib" }

# dev
gleam_erlang = ">= 1.3.0 and < 2.0.0"
# gleam_erlang = { path = "../gleam_erlang" }

input = ">= 1.0.1 and < 2.0.0"
simplifile = ">= 2.3.1 and < 3.0.0"
envoy = ">= 1.0.2 and < 2.0.0"
# glexec = { path = "../gleam_erlexec"}
glexec = { git = "https://github.com/tamadamas/gleam_erlexec.git", ref = "master" }

[dev-dependencies]
gleeunit = "~> 1.0"
